apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  labels:
    app: todo-api
data:
  # Données non sensibles uniquement : hôte, port, nom de la base.
  # Les credentials (DB_USER, DB_PASSWORD) sont dans le Secret.
  DB_HOST: postgres-service
  DB_PORT: "5432"
  DB_NAME: todo_db
