apiVersion: v1
kind: Secret
metadata:
  name: todo-secrets
  labels:
    app: todo-api
type: Opaque
stringData:
  # In real projects, avoid plain text in git and use a secret manager.
  DB_PASSWORD: postgres
  POSTGRES_PASSWORD: postgres
